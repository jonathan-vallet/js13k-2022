function startGame() {
  characterList = [];
  currentCardIndex = 0;

  // Adds characters at start to see "deck"
  addCharacter();
  addCharacter();
  addCharacter();
  addCharacter();
}

// Let's the game start!
startGame();
